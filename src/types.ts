export enum Operator {
  ADD = '+',
  SUBSTRACT = '-',
  MULTIPLY = '*',
  DEVIDE = '/',
};

export enum TokenType {
  Numeric = 'Numeric',
  Operator = 'Operator',
  Space = 'Space',
  Error = 'Error',
};
